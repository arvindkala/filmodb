 Instructions to run :

 * Clone the github repo.
 
 * run `npm install` inside the cloned folder.

 * Change the DB user and password in config.js

 * You can run the app with `node app.js`

 * Browse the functionality using `localhost:3000`
